import { FiltroEmergenciasPipe } from './filtro-emergencias.pipe';

describe('FiltroEmergenciasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEmergenciasPipe();
    expect(pipe).toBeTruthy();
  });
});
